Gabgool — Multiplayer (rooms)

Run locally:

```bash
npm install
npm start
# open http://localhost:3000 in your browser and use the Start screen to create or join a room
```

Notes:
- Use the Start screen to create a room (generates a code) or enter an existing room code.
- Players in the same room see the same mob and player scores update in real time.
